# Port the Express server listens on.
# Most hosts (Render, Railway, Heroku, Fly) inject PORT automatically.
PORT=5180

# Public base URL of the deployment — used to build absolute URLs in
# robots.txt and sitemap.xml. Optional; falls back to the request host.
# BASE_URL=https://barha.example

NODE_ENV=production
